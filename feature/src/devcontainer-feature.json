{
  "id": "init",
  "version": "1.0.4",
  "name": "Init",
  "description": "Initialize development environment with configurable options",
  "documentationURL": "https://github.com/ghostmind-dev/features/tree/main/features/src/init",
  "postCreateCommand": "deno run -A -r jsr:@ghostmind/init",
  "capAdd": ["NET_ADMIN", "IPC_LOCK"],
  "mounts": [
    {
      "source": "/run/host-services/ssh-auth.sock",
      "target": "/ssh-agent",
      "type": "bind"
    },
    {
      "source": "${env:HOME}/.ssh/known_hosts",
      "target": "/home/vscode/.ssh/known_hosts",
      "type": "bind"
    },
    {
      "source": "${env:HOME}/.ssh/config",
      "target": "/home/vscode/.ssh/config",
      "type": "bind"
    },
    {
      "source": "${localEnv:RUN_PROJECT}",
      "target": "${localEnv:RUN_PROJECT}",
      "type": "bind"
    },
    {
      "source": ".history",
      "target": "/commandhistory",
      "type": "bind"
    }
  ],
  "containerEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },
  "options": {
    "resetLive": {
      "type": "boolean",
      "default": false,
      "description": "Reset live environment settings (default: false)"
    },
    "baseZshrc": {
      "type": "boolean",
      "default": true,
      "description": "Configure base ZSH configuration (default: true)"
    },
    "denoConfig": {
      "type": "boolean",
      "default": true,
      "description": "Setup Deno configuration (default: true)"
    },
    "coreSecrets": {
      "type": "boolean",
      "default": true,
      "description": "Setup core secrets management (default: true)"
    },
    "loginNpm": {
      "type": "boolean",
      "default": false,
      "description": "Configure NPM login (default: false)"
    },
    "loginGcp": {
      "type": "boolean",
      "default": true,
      "description": "Configure Google Cloud Platform login (default: true)"
    },
    "loginGhcr": {
      "type": "boolean",
      "default": true,
      "description": "Configure GitHub Container Registry login (default: true)"
    },
    "loginVault": {
      "type": "boolean",
      "default": true,
      "description": "Configure HashiCorp Vault login (default: true)"
    },
    "loginCloudflare": {
      "type": "boolean",
      "default": true,
      "description": "Configure Cloudflare login (default: true)"
    },
    "pythonVersion": {
      "type": "string",
      "default": "3.12.7",
      "description": "Python version to configure (default: 3.12.7)"
    },
    "tmuxConfig": {
      "type": "boolean",
      "default": true,
      "description": "Setup TMUX configuration (default: true)"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils:2"]
}
